import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
`;

const Post = () => {
  return (
    <Wrapper>
      <h6>Post</h6>
    </Wrapper>
  );
};

export default Post;
